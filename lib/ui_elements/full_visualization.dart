import 'package:flutter/material.dart';

class FullVisualization extends StatelessWidget {
  const FullVisualization({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("4444444444444"),
    );
  }
}
