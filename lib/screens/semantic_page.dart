import 'package:flutter/material.dart';

class SemanticPage extends StatelessWidget {
  const SemanticPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("333333333333"),
    );
  }
}
